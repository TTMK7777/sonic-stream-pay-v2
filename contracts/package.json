{
  "name": "sonic-stream-pay-contracts",
  "version": "1.0.0",
  "description": "Streaming payment smart contracts for Sonic blockchain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network sonicTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network sonicMainnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
